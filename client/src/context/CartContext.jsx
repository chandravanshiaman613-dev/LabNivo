import { createContext, useContext, useMemo, useState } from 'react'
const CartContext = createContext(null)
export function CartProvider({ children }) { const [items,setItems]=useState([]); const [open,setOpen]=useState(false); const add=item=>setItems(current=>current.some(x=>x.slug===item.slug)?current:[...current,item]); const remove=slug=>setItems(current=>current.filter(x=>x.slug!==slug)); const setCouple=(slug, enabled)=>setItems(current=>current.map(item=>item.slug===slug?{...item,couple:enabled,person2:enabled?(item.person2||{name:'',age:'',gender:''}):undefined}:item)); const updatePerson2=(slug,person2)=>setItems(current=>current.map(item=>item.slug===slug?{...item,person2}:item)); const clear=()=>setItems([]); const value=useMemo(()=>({items,add,remove,setCouple,updatePerson2,clear,open,setOpen,total:items.reduce((sum,item)=>sum+item.price+(item.couple?Math.max(0,item.price-100):0),0)}),[items,open]); return <CartContext.Provider value={value}>{children}</CartContext.Provider> }
export const useCart = () => useContext(CartContext)
