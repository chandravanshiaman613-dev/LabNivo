import { useState } from 'react'
import { NavLink, Link } from 'react-router-dom'
import { useCart } from '../../context/CartContext'
import { BUSINESS_WHATSAPP_NUMBER, whatsappLink } from '../../config/business'
import './Navbar.css'
export default function Navbar(){const {items,setOpen}=useCart();const [open,setMenuOpen]=useState(false);const close=()=>setMenuOpen(false);return <><div className="contact-bar"><span>Call us: <a href={`tel:${BUSINESS_WHATSAPP_NUMBER}`}>{BUSINESS_WHATSAPP_NUMBER}</a></span><a href={whatsappLink('Hello LAB NIVO, I would like help booking a diagnostic test.')} target="_blank" rel="noreferrer">WhatsApp</a><span>Home Sample Collection</span></div><header className="navbar"><Link className="brand" to="/"><span className="brand-mark">N</span><span>LAB <b>NIVO</b><small>Diagnostics at Your Doorstep</small></span></Link><nav className={open?'mobile-open':''}><NavLink onClick={close} to="/">Home</NavLink><NavLink onClick={close} to="/tests">Tests</NavLink><NavLink onClick={close} to="/packages">Packages</NavLink><NavLink onClick={close} to="/upload-prescription">Upload Prescription</NavLink><NavLink onClick={close} to="/my-orders">My Orders</NavLink><Link onClick={close} to="/#contact">Contact</Link><a className="nav-whatsapp" href={whatsappLink('Hello LAB NIVO, I would like help booking a diagnostic test.')} target="_blank" rel="noreferrer">Book on WhatsApp</a></nav><div className="nav-controls"><button className="cart-button" onClick={()=>setOpen(true)}>Cart <span>{items.length}</span></button><button className="menu-button" aria-label="Open navigation menu" aria-expanded={open} onClick={()=>setMenuOpen(x=>!x)}>☰</button></div></header></>}
