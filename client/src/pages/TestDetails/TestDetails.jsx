import { useEffect, useState } from 'react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { getTestBySlug } from '../../services/testService'
import { money } from '../../utils/catalogue'
import { useCart } from '../../context/CartContext'
import { whatsappLink } from '../../config/business'
import './TestDetails.css'
export default function TestDetails(){const {slug}=useParams(),[test,setTest]=useState(null),[error,setError]=useState('');const {add}=useCart();const navigate=useNavigate();useEffect(()=>{getTestBySlug(slug).then(setTest).catch(()=>setError('Unable to load this test. Please try again.'))},[slug]);if(error)return <main className="detail-page"><p className="error-state">{error}</p></main>;if(!test)return <main className="detail-page">Loading test...</main>;const item={...test,type:'TEST',reference:test._id,price:test.sellingPrice};return <main className="detail-page"><Link className="back-link" to="/tests">← All tests</Link><span className="test-category">{test.category}</span><h1>{test.name}</h1><p>{test.shortDescription}</p><div className="detail-price"><span className="mrp">{money(test.mrp)}</span><strong>{money(test.sellingPrice)}</strong></div><div className="detail-meta"><div><b>Sample type</b><span>{test.sampleType}</span></div><div><b>Preparation</b><span>{test.preparation}</span></div><div><b>Report timeline</b><span>{test.reportTAT}</span></div></div><button className="button button-primary" onClick={()=>{add(item);navigate('/book')}}>Book Test</button><a className="button button-secondary" href={whatsappLink(`Hello LAB NIVO, I want to book: Test: ${test.name} Price: ${money(test.sellingPrice)} Patient Name: Mobile: Preferred Date: Preferred Time: Address: Please confirm the booking.`)} target="_blank" rel="noreferrer">Book on WhatsApp</a></main>}
