import { useEffect, useState } from 'react'
import { useParams } from 'react-router-dom'
import { getMyOrder } from '../../services/orderService'
import { money } from '../../utils/catalogue'
const steps=['Booking Confirmed','Sample Collection Scheduled','Sample Collected','Testing in Progress','Report Ready'];
export default function OrderDetails(){const {bookingId}=useParams(),[order,setOrder]=useState(null),[error,setError]=useState('');useEffect(()=>{getMyOrder(bookingId).then(setOrder).catch(e=>setError(e.response?.data?.message||'Unable to load booking.'))},[bookingId]);if(error)return <main className="booking-page"><p className="form-error">{error}</p></main>;if(!order)return <main className="booking-page">Loading booking...</main>;const cancelled=order.status==='Booking Cancelled';const active=Math.max(0,steps.indexOf(order.status));return <main className="booking-page"><span className="overline">BOOKING DETAILS</span><h1>{order.bookingId}</h1><section className="booking-form"><p><b>Patient:</b> {order.customer.name}</p><p><b>Tests:</b> {order.items.map(i=>i.name).join(', ')}</p><p><b>Amount:</b> {money(order.totalAmount)}</p><p><b>Collection:</b> {new Date(order.preferredDate).toLocaleDateString('en-IN')} · {order.preferredTimeSlot}</p><p><b>Address:</b> {order.address}</p><p><b>Status:</b> {order.status}</p></section><section className="timeline"><h2>Status tracking</h2>{cancelled?<div className="active">● Booking Cancelled</div>:steps.map((step,index)=><div className={index<active?'done':index===active?'active':''} key={step}>● {step}</div>)}</section></main>}
