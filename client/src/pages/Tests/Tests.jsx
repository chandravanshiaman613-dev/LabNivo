import { useEffect, useState } from 'react'
import TestCard from '../../components/TestCard/TestCard'
import { getTests } from '../../services/testService'
import './Tests.css'
const categories=['All tests','Hematology','Diabetes','Thyroid','Liver','Vitamins']
export default function Tests(){const [search,setSearch]=useState(''),[category,setCategory]=useState(''),[tests,setTests]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{setLoading(true);setError('');getTests({...(search&&{search}),...(category&&{category}),status:'active'}).then(setTests).catch(()=>setError('Unable to load tests. Please try again.')).finally(()=>setLoading(false))},[search,category]);return <main className="catalogue-page"><span className="overline">TEST CATALOGUE</span><h1>Find a diagnostic test</h1><p>Browse common doctor-prescribed tests. Prices shown are demo prices.</p><div className="search-box"><span>⌕</span><input value={search} onChange={event=>setSearch(event.target.value)} placeholder="Search CBC, Thyroid, HbA1c, Widal..."/><button>Search</button></div><div className="chips">{categories.map(item=><button className={category===item||(!category&&item==='All tests')?'selected':''} key={item} onClick={()=>setCategory(item==='All tests'?'':item)}>{item}</button>)}</div>{loading?<p className="catalogue-state">Loading tests...</p>:error?<p className="catalogue-state error-state">{error}</p>:tests.length?<div className="test-grid">{tests.map(test=><TestCard key={test.slug} test={test}/>)}</div>:<p className="catalogue-state">No tests found.</p>}</main>}
